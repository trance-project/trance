import {makeStyles} from "@material-ui/core";

export const queryBuilderThemeStyle = makeStyles(theme => ({
    queryBtnGroup: {
        float:"right",
        marginRight: 10
    },
    iconView:{
        float:"right",
        display:"inline-block",
        position:'relative'
    }
}));