import {makeStyles} from '@material-ui/core/styles';

const AlertNotificationThemeStyle = makeStyles(themes =>({
    queryBtnGroup: {
        float:"right",
        marginRight: 10
    }
}));

export default AlertNotificationThemeStyle;