package models

import java.util.UUID

/**
  * Model for the Object to be sent to trance webapp to define databases object information
  */
case class TableObjects(
                       _id: Option[UUID],
                       
                       )
