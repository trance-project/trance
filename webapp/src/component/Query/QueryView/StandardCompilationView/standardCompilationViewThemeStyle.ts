import {makeStyles} from '@material-ui/core/styles';

export const standardCompilationViewThemeStyle = makeStyles(themes =>({
    queryBtnGroup: {
        float:"right",
        marginRight: 10
    }
}));